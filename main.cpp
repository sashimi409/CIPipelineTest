#include <iostream>
#include "Printer.h"

int main() {

	// Prints a value for everynumber from 1 to 100
	// Bases on what it is a multiple of
	for (int Num = 1; Num <= 100; Num++)
	{
		PrintValue(Num);
	}

	return 0;
}